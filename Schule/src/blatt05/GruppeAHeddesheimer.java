package blatt05;

import java.util.Scanner;

public class GruppeAHeddesheimer {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        // Einlesen der Punkten, max und erreichte
        System.out.print("Geben Sie die Anzahl der Noten ein: ");
        int anzahl = input.nextInt();
        int erfahrung = anzahl;
        int notenwerte =0;
        while (erfahrung >0) {
            System.out.print("Bitte geben Sie ihre Note ein: ");
            int note = input.nextInt();

        }
    }
}
